# test1

A simple Python sample project for `firstPgm.py`.

This project contains a basic script that asks whether the user is a regular student and prints the appropriate next steps.

## Usage

```bash
python firstPgm.py
```

Then answer the prompt with `yes` or `no`.

## File

- `firstPgm.py`: main script for the user prompt and conditional output.
